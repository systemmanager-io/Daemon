image: sandrokeil/typescript

cache:
  key: "SystemManager-Daemon"
  paths:
    - "node_modules/"


stages:
  - build
  - test

build:
  stage: build
  script:
    - yarn
    - yarn build
    - rm .git -rf
  artifacts:
    paths:
      - './'

test:
  stage: test
  script:
    - echo "No tests are defined yet"
